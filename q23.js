function myFunction(num1, num2) {
  result = num1+num2
}

let result = 0
myFunction(100, 2)
console.log(result)

// I expected this to be 0 like the previuous question as let is stating the result being 0
// However I now see that the result should be from what is inside the function  