function myFunction(num1) {
  return -100

  return num1 * 10
}

const result = myFunction(5)
console.log(result)

// The function returns -100 therefore even if myFunction is giving value 5

// The console log should still show -100 as that is what is returning inside the function 